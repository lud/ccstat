publish = false

pre-release-hook = ["sh", "-c", "git cliff -o CHANGELOG.md --tag v{{version}} && git add CHANGELOG.md"]

pre-release-commit-message = "chore: release v{{version}}"
tag-message = "v{{version}}"
