# ccstat — Claude Code instructions

## After making changes

Always rebuild and install with:

```sh
just install
```

This runs `cargo build --release` and copies the binary to `~/.local/bin/ccstat`.
